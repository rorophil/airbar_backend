name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.3.1'
      SERVERPOD_PASSWORD_database: 'dHNjhnRTbV73Nwd7VAabLVLl740Ht2Aj'
      SERVERPOD_PASSWORD_redis: 'aOo7byqFbKR0JbF-CBG4q7AKIwvhGUjt'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: airbar_backend_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: airbar_backend_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: airbar_backend_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: airbar_backend_server
        run: serverpod generate

      - name: Run tests
        working-directory: airbar_backend_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: airbar_backend_server
        run: docker compose down -v
